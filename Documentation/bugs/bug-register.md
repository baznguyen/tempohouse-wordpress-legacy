# Bug Register

| ID | Severity | Symptom | Root Cause | Files Changed | Verified | Commit |
|----|----------|---------|------------|---------------|----------|--------|
| BUG-001 | Medium | Hard horizontal amber line visible above tempo-frame on landscape aspect ratios — downlight wall-flood glow appeared clipped at a straight edge | `::after` element top was -88px but gradient center used percentage (`50% 10%`) making it frame-height-dependent. For landscape frames (~400px) the gradient faded to 0.24 opacity amber right at the element top, producing a visible cut line. NOT an overflow clip issue. | `assets/css/components/tempo-frame.css` (top: -88px→-115px, center at 50% 43px px-fixed, added mask-image soft-fade) | Visual: amber line absent on hover across portrait, landscape, square frames on cafe, gallery, bar pages | 07f7818-era, shipped v3.60.2 |
| BUG-002 | Medium | `.page-bar__programme` section clipped the cocktail programme frame's ceiling lamp (::before at top:-72px) and wall flood (::after at top:-115px) with a hard edge | `overflow: hidden` creates a scroll container that clips positioned children extending outside parent bounds | `assets/css/pages/bar.css` (overflow: hidden → overflow: clip; overflow-clip-margin: 110px) | Visual: lamp and flood visible above frame on bar page programme section | Shipped v3.60.3 |
| BUG-003 | Low | Gallery walk mobile carousel prev/next dots and buttons appeared non-functional; carousel did not auto-advance | No actual click-handler bug — buttons worked but no auto-advance existed. User expected slideshow behaviour by analogy with other sections. | `assets/js/page-gallery.js` (added startAuto/stopAuto, 4000ms setInterval; pause on touchstart/scroll, restart after settle) | Live: carousel advances every 4s; touch/drag pauses and resumes after scroll settles; prev/next and dot clicks reset the timer | Shipped v3.60.3 |
| BUG-004 | Medium | Moods mobile carousel wall flood showed a hard amber edge above the frames | Same root cause as BUG-001: `::before` used `at 50% 10%` (percentage). At 10% of element height (41px), gradient center was 27px BELOW the lamp (14px from element top), leaving ~0.29 opacity amber at element top. Compounded by CSS spec: `overflow-x: auto` forces `overflow-y: clip` → `hidden`, killing the 110px clip margin. | `assets/css/components/moods.css` (::before top: -88px→-115px; frames-wrap padding-top: 94px→120px for 5px clearance; gradient center `at 50% 10%` → `at 50% 41px`; added mask-image soft-fade 0→30px) | Visual: smooth radial falloff above lamp, no hard edge on homepage moods carousel mobile | Shipped v3.61.1 |
| BUG-005 | Medium | Homepage events carousel and What's On masonry list clipped the wall flood glow at a hard horizontal line near the card top | Two compounding issues: (1) `.events { overflow: hidden }` on the section clipped the glow that the viewport's `overflow-y: clip; overflow-clip-margin: 110px` was trying to allow above cards — the glow extended ~110px above viewport but only ~100px below section top, so section clipped it 10px shy. (2) `event-card::before` used same `at 50% 10%` percentage gradient bug creating visible amber at element top. | `assets/css/components/events.css` (section `overflow: hidden` → `overflow: visible`; `::before` top: -88px→-115px; gradient center `at 50% 10%` → `at 50% 41px`; added mask-image soft-fade 0→30px) | Visual: smooth lamp glow on homepage events carousel and What's On masonry, no section boundary clip | Shipped v3.61.2 |
| BUG-006 | Medium | What's On page carousel prev/next arrows and dots rendered in DOM but had no click response; `whats-on.js` initialised nothing | WordPress CPT archive filter routes `/whats-on/` to `page-templates/page-whats-on.php` (not the root `page-whats-on.php`). The nav HTML (`<nav class="page-whats-on__carousel-nav">`) existed only in the root template, so `whats-on.js` found no `.page-whats-on__carousel-nav` inside each section and bailed early on `if (!nav) return`. | `page-templates/page-whats-on.php` (added carousel nav HTML with prev/next SVG arrows and PHP-generated dots after each `.events__viewport--grid`, guarded by `post_count > 1`) | Confirmed: `has_nav: true`, `dots_count: 3`, `scroll_before: 0 → scroll_after: 336`, `moved: true` in Chrome devtools after fix | Shipped v3.63.1 |
| BUG-008 | Medium | Catering & Partners art frame expanded massively at mid-viewport widths, pushing the FAQ section heading to overlap catering content | `.page-events__catering-img` had both `aspect-ratio: 3/4` (base rule, line 532) and `align-self: stretch` (override rule, line 1118) active simultaneously. In CSS Grid, these two compete: `align-self: stretch` lets the grid set height, but `aspect-ratio: 3/4` then recalculates width or height depending on browser resolution order, producing a frame equal to column-width × 4/3 (~660px) instead of matching the text column height. Also: at ≤768px (stacked column) the `flex: 1` chain on mat/artwork had no defined parent height to fill, leaving the frame at an unpredictable size. | `assets/css/pages/events-pages.css` (added `aspect-ratio: unset` to `.page-events__catering-img` override; added `@media (max-width: 768px)` block resetting `align-self: auto; min-height: unset` on frame and restoring `aspect-ratio: 4/3; flex: none` on artwork) | Visual: frame height matches catering text column height on desktop; 4:3 proportioned frame on mobile stacked layout | Shipped v3.65.1 |
| BUG-007 | Medium | Works on Paper (landscape event card) showed a blank cream band at the bottom of the artwork and title-bar floated in empty space. Landscape image (800×500) renders 206px tall at column width, but `min-height: 240px` on `.event-card__artwork` forced the container 34px taller; `overflow: hidden` then clipped nothing but the blank gap was visible. Title-bar (`position: absolute; bottom: 0`) sat in the empty zone. Also, all 3 cards were the same column width so the landscape frame looked undersized. | `min-height: 240px` on `.events__viewport--grid .event-card__artwork` (intended as ghost-card fallback, incorrectly applied to all cards including those with images). Secondary: `column-count: 3` (equal widths) made the short landscape card look visually weak next to tall portraits. | `assets/css/pages/whats-on.css` (replaced `column-count: 3` with 4-column CSS Grid; portrait cards default `span 1`, landscape cards `[data-orientation="landscape"]` get `span 2`; removed `min-height: 240px`; added `:not(:has(.event-card__media-layer)) { min-height: 280px; aspect-ratio: 3/4 }` ghost fallback); `page-templates/page-whats-on.php` (added `data-orientation="portrait/landscape"` via `wp_get_attachment_metadata()` in both Happening Now and Coming Up loops) | Verified: `grid_cols: 281px×4`, portrait `artwork_h === img_h` (305=305), landscape `artwork_h === img_h` (331=331) — zero blank space in any card | Shipped v3.64.0 |
| FP-001 | High | Dragging an element into a zone that already contained other elements showed "no space available" error and the dropped element was rejected — it did not join the zone. Root cause: `findFreePositionNear` used 8px step vs 24px snap grid, so nudged position didn't land on grid; zone membership check used nudged position (which was outside zone bbox after repositioning) not the user's original drop intent. Secondary: zone membership PATCH used `furniture/` endpoint even in layout mode where IDs are slot IDs. | (1) `findFreePositionNear`: step = `S.snapEnabled ? 24 : 8`, start from `snapToGrid(cx/cy)` — all candidates on grid, no re-snap needed. (2) Capture `_intentX/_intentY` before nudge; use these for zone membership check. (3) `var _zEndpoint = S.activeLayout ? 'slots/' + z.id : 'furniture/' + z.id`. | `assets/js/floor-plan-builder.js` (dragend handler ~lines 1320–1430) | Node.js simulation confirmed; confirmed visually in Docker | Fixed 2026-06-18 |
| FP-002 | Low | Elements placed adjacent were nearly touching with only ~4px visual gap, making it difficult to distinguish separate tables on the floor plan. | `aabbOverlap` pad was 4px — minimum gap between element bounding boxes was 4px, visually insufficient. | `assets/js/floor-plan-builder.js` (`isOverlappingAny` → `aabbOverlap` pad parameter: 4→16) | Visual: tables have clear 16px breathing room between them | Fixed 2026-06-18 |
| FP-003 | High | All intermediate changes (drag, zone membership, group join, rename, joinable toggle) were writing to DB immediately via `apiFetch PATCH`. Changes could not be abandoned; there was no staged publish workflow. Also `saveLayout()` base-mode patch was missing `group_id` and `is_combinable`. | Deferred save architecture: 10 intermediate PATCH calls removed from dragend, joinSelected/separateSelected/leaveGroup (rewritten synchronous), saveZone, zone rename dblclick, zone group drag, joinable override. `saveLayout()` now returns its Promise and includes `group_id`+`is_combinable` in base-mode patch. | `assets/js/floor-plan-builder.js` (lines ~1388, 1409, 1829, 1876, 1971, 2000, 2021, 2537/2541, 2725, 3047–3080) | Confirmed: no PATCH fires on drag/join/zone actions; Publish button sends all changes atomically | Fixed 2026-06-18 |
